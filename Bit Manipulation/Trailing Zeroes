public class Solution {
    public int solve(int A) {
        int count = 0;
        
        while(A > 0) {
            int rem = A % 2;
            if(rem == 0) count++;
            else return count;
            A /= 2;
        }
        return count;
        
    }
}
